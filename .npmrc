# The Angular 20.3.x toolchain has a hard Node.js preflight check in @angular/cli.
# Without engine-strict, the `engines` field in package.json is only an EBADENGINE
# warning that scrolls past during install, and the real failure surfaces much later
# as a confusing mid-build error from `ng`. This makes it a hard failure at install time.
engine-strict=true
